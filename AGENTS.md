# SharedAutonomy-VLA Agent Rules

本文件只记录进入仓库后必须遵守的工程边界。项目背景、架构和使用说明见 `README.md`；代码风格、日志和配置约定见 `docs/engineering_conventions.md`。

## 基本原则

- 默认使用中文解释；代码、变量名、命令、日志和技术术语保持英文。
- 本项目 Windows 端默认 Conda 环境为 `sharedautonomy-lr060-cf`。涉及 LeRobot、RealMan SDK、硬件连通性或项目测试时，优先使用该环境；不要使用已知存在问题的旧环境 `sharedautonomy-lr060`。
- 修改前先阅读相关实现与配置；优先做最小、局部、可验证的修改。
- 不主动引入依赖，不做与当前任务无关的重构、重命名或格式化。
- 每次修改后运行与改动相称的测试；若未验证，必须明确说明。
- 仓库内文本文件使用 UTF-8 编码。

## 真机安全与硬件边界

- 机械臂运动必须默认关闭；除非用户明确要求，不得启用运动、夹爪闭合或其他物理执行器。
- Codex 沙箱内的局域网探测或硬件 SDK 连接失败，不能直接判定设备离线。若用户已从本机确认设备可达，应在说明只读范围并取得授权后，在沙箱外使用 `sharedautonomy-lr060-cf` 重试；不得改用旧环境规避问题。
- 真实硬件 SDK、相机和 SpaceMouse 依赖必须 lazy import；没有硬件时，普通 import、单元测试和离线工具不应失败。
- 真机接口改动必须保留 mock 或纯函数测试路径，且不得绕过 `sharedautonomy.robot.safety` 的安全检查。
- 新建或调整配置时，不在共享配置或文档中新增机器本地信息，例如实际 IP、串口、相机序列号、标定结果、磁盘路径、token 或密钥；这些信息属于 `configs/local/` 或环境变量。现有临时连接配置的迁移应单独完成并验证。
- 不得批量删除文件或目录；禁止使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。

## 数据、配置与输出

- 运行时配置必须可追溯；后续 runner 应保存 effective config、代码版本和必要的设备元数据。
- 高频控制数据写入结构化 episode recorder，不使用普通文本日志逐步打印。
- `data/`、`datasets/`、`recordings/`、`outputs/`、模型权重及本地配置均视为运行产物，除非明确要求，不得修改或删除。

## 文档与每日工作流

项目文档按“README、路线图、每日计划、每日日志、长期主题文档”分层维护。进入仓库后，不需要用户重复提醒以下规则。

### 文档职责

- `README.md` 只维护长期稳定的项目介绍、架构、使用入口和文档导航，不放每日任务清单或详细实验流水。
- `docs/roadmap.md` 维护阶段级目标、当前进度和验收标准，不展开成当天的操作步骤。
- `docs/daily/YYYY-MM-DD/plan.md` 是对应日期任务状态的唯一来源。
- `docs/daily/YYYY-MM-DD/log.md` 是对应日期实际工作、实验结果和结论的历史记录。
- `docs/hardware_setup.md`、`docs/engineering_conventions.md` 和 `docs/decisions/` 等主题文档是长期技术事实与设计决策的权威来源。
- 每日文档的具体格式和流程见 `docs/daily/README.md`，新文件优先复用 `docs/daily/templates/`。

### 开始和执行任务

- 开始有实质内容的项目工作前，先阅读 `docs/roadmap.md`、最近一个工作日的 `log.md` 和当前日期已有的 `plan.md`。
- 若当前日期尚无计划且用户准备开展当天工作，应根据路线图和上一工作日日志创建 `docs/daily/YYYY-MM-DD/plan.md`。
- 工作过程中同步维护当天 `plan.md` 的 checkbox；未完成事项必须注明延期、取消或阻塞原因，不能直接消失。
- 不因一次很小的问答自动新建每日目录；只有当天确实开展项目工作、用户要求安排当日任务，或进入收尾整理时才创建。

### 每日收尾

- 当用户表示整理当天工作、准备提交、结束当天工作或询问当天是否完成时，主动核对并更新当天 `plan.md` 和 `log.md`。
- `log.md` 至少包含：完成情况、实验与结果、新结论与决策、今日理解重点、代码与文档变更、验证、未完成与阻塞、已沉淀到长期文档、下一工作日建议。
- 可长期复用的结论必须同步整理进对应主题文档；日志只保留摘要和链接，避免多个权威副本。
- “下一工作日建议”是候选项，下一工作日的 `plan.md` 才是正式任务清单。
- 日志完成后原则上不重写历史，仅修正明确事实错误或补充必要链接。

### 今日理解重点

- 每日日志固定维护“今日理解重点（15–30 分钟）”，帮助用户理解由 AI 辅助完成的关键实现。
- 每天只选 3–5 个最重要概念，优先解释设计原因、关键取舍、代码如何实现和当前限制，不做完整改动复述。
- 每项尽量包含：一句话解释、为什么重要、本项目怎么做、代码入口和一个可脱离 AI 回答的自测问题。
- 末尾提供简短的面试式自测题与关键词提示，避免只给需要死记的完整答案。
- 若当天主要是文档、实验或硬件工作，也应选择相应的关键概念，不得因为代码量少而省略这一节。

### 一致性与验证

- 修改文档结构或移动内容后，检查 README 导航、路线图、每日索引和相对链接。
- 已验证事实、推断和待验证事项必须明确区分；不能为了让当日清单看起来完成而把未接入运行时或未做真机验收的功能标为完成。
- Markdown 日志不保存高频原始数据、完整终端输出、真实 IP、序列号、标定矩阵或其他本机敏感配置。
