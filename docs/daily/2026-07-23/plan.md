# 2026-07-23 工作计划

## 今日目标

降低项目继续推进前的硬件、控制接口、数据接口和安全边界不确定性，并明确下一条可验证链路。

## 完成标准

- [x] 明确 RM-65B、SpaceMouse、夹爪和腕部 D435i 的当前能力边界；
- [x] 获得慢速遥操作所需的关键频率和延迟基线；
- [x] 定义运行时数据接口和统一时间戳语义；
- [x] 定义固定竖直夹爪的任务空间边界和桌面下限；
- [x] 将重要结论写入长期文档；
- [x] 相关离线测试和静态检查通过。

## 任务清单

### 仓库与工程约定

- [x] 创建仓库和目录骨架；
- [x] 配置 `.gitignore` 和 Python 项目；
- [x] 确定代码风格、日志和配置方式。

### 数据接口

- [x] 定义 `RobotObservation`；
- [x] 定义 `HumanAction`；
- [x] 定义 `AssistAction`；
- [x] 定义 `ExecutedAction`；
- [x] 定义 `EpisodeMetadata`；
- [x] 定义相机和机器人统一时间戳。

### 硬件与延迟

- [x] 确认 RM-65B 状态反馈和控制频率；
- [x] 确认 SpaceMouse 输入频率和 50 Hz 消费新鲜度；
- [x] 确认腕部 D435i profile、稳定性和软件链路年龄；
- [x] 确认夹爪动作接口边界；
- [x] 测试 MoveJ、低跟随 CAN-FD 和高跟随 CAN-FD 响应；
- [x] 完成 SpaceMouse → deadman → safety clipping → RM-65B 的 J6-only 联调；
- [x] 验证示教器软件急停和关节限位。

### 任务空间安全

- [x] 复用并核对旧 `stamp` 的夹爪长度和桌面坐标约定；
- [x] 确定四点凸多边形 XY 工作区；
- [x] 确定固定竖直夹爪和最低法兰 `Z=178 mm`；
- [x] 实现 Cartesian 工作区、真实 `dt` 限幅、固定姿态和输入新鲜度纯函数；
- [x] 完成纯函数测试。

### 文档整理

- [x] 将每日任务从主 README 迁出；
- [x] 建立路线图、每日计划和每日日志结构；
- [x] 整理本日工作日志和下一工作日建议。

## 延期但不阻塞今日完成

- ~~固定第三视角相机尚未购买~~（2026-07-24 已到货并完成软件侧接入；见当日 plan）；
- ~~Cartesian 安全过滤尚未接入实际 runner~~（2026-07-24 已完成；见当日 plan）；
- 夹爪严格物理延迟和相机严格光学延迟需要外部基准；
- ~~完整 episode recorder、回放和可视化链路留到后续~~（2026-07-24 已完成 recorder / 同步观测 / 加载回放接口；可视化回放仍待后续）。

## 今天不做

- 训练 ACT/VLA；
- 采集正式数据集；
- 执行未经当前计划明确授权的 Cartesian 真机运动；
- 为非关键延迟指标搭建外部高速摄影或光学基准。

