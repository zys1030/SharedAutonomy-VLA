# 2026-07-31 工作计划（Day 7）

## 今日目标

完成 `act_c0_r3` 真机复测与失败模式归因，判断 C0 下一步；下午出差提前收工。

## 完成标准

- [x] `train-091`…`100` 已 `--resume` 进 `shape_pick_place_v1_c0`（40 ep），未覆盖旧快照 / r2；
- [x] `act_c0_r3` 训完并可 rollout；
- [x] 真机复测 + `reset_every` 诊断 + 水平偏差格点表 + 闭合 EE 散点；
- [x] 形成采集判决：+20（14 clean 分层抖动 + 6 水平修正），C1/VLA 暂缓；
- [ ] `act_c0_r4` 采集与训练——下午出差延期；
- [x] 同步 `docs/datasets.md` / `docs/roadmap.md` / 本日 log。

## 任务清单

### P0：今天必须完成

- [x] C0 40 条 / `act_c0_r3` 复测与失败归因；
- [x] `reset_every=10/15/25`：保留 25；
- [x] 水平偏差格点表 + `plot_grasp_close_xy.py` 散点；
- [x] 写下一批采集配方与停损线；
- [ ] 执行 +20 采集与重训（延期）。

### P1：有时间再做

- [ ] Week 2 SA 采集器脚手架——延期；
- [ ] 失败上下文导出——延期；
- [x] `reset_every=10` 评估（已排除）。

## 开始前条件

- [x] `sharedautonomy-lr060-cf` / 服务器 `act_c0_r3` 可用；
- [x] c0 40 条已同步；旧 checkpoint 只读；
- [x] 真机双确认 + `reset_every=25`。

## 今天不做

- C1 / `_v004` / VLA 当主线；
- 无差别堆数据或只采格点中心；
- 改 runner / temporal ensemble 治打圈。

## 待决策（已决）

- [x] C0 40 条后不收口，不进 C1；
- [x] 下一优先是水平对准，不是把手高度悬停专项；
- [x] 下一批 +20（14+6），评估用格点、训练用抖动；
- [x] VLA 微调不当当前捷径。
